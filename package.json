{
  "name": "early-pay-terms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/inject-sw.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "tsc --noEmit && vitest run tests/calculator.test.ts && npm run build && playwright test",
    "test:unit": "vitest run tests/calculator.test.ts",
    "test:e2e": "CI=1 playwright test",
    "test:stable": "CI=1 npm test && CI=1 npm test"
  },
  "dependencies": {
    "vite": "^7.3.6"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "@types/node": "^24.3.0",
    "typescript": "5.9.2",
    "vitest": "^3.2.7"
  }
}
